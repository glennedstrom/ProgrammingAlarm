from typing import List

def sort_list(numbers: List[int]) -> List[int]:
    """
    Sort the given list of integers in ascending order.
    
    Args:
        numbers (List[int]): A list of integers. Expected to contain non-negative integers with a maximum length of 10^6.
        
    Returns:
        List[int]: A new list containing the integers in ascending order.
        
    Raises:
        ValueError: If 'numbers' is empty or contains non-integer elements.
    """
    # Your code here
    pass

